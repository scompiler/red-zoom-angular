import { NgxRedZoomDirective } from './ngx-red-zoom.directive';

describe('NgxRedZoomDirective', () => {
  it('should create an instance', () => {
    const directive = new NgxRedZoomDirective();
    expect(directive).toBeTruthy();
  });
});
